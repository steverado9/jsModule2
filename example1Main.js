//Whenever we are importing in plain javascript, we must use the extension .js
import { sum, sub  } from './example1Calc.js'; //syntax for importing by name//

console.log(sum(2,3)); //calling the function sum with two arguments.
console.log(sub(10,5)); //calling the function sub with two arguments.

